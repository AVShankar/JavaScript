function isSameLength(word1, word2)
{
    var len1 = word1.length;
    var len2 = word2.length;
    if(len1==len2)
    {
        console.log("true");
    }
    else
    {
        console.log("false");
    }
}
var str1="GUVI", str2="GEEK";
isSameLength(str1, str2);